{
  "name": "pets-website",
  "version": "1.0.0",
  "private": true,
  "description": "MERN Pet Marketplace — monorepo runner for server, client and admin",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix admin",
    "seed": "npm run seed --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:admin": "npm run dev --prefix admin",
    "dev": "concurrently -n SERVER,CLIENT,ADMIN -c green,cyan,magenta \"npm:dev:server\" \"npm:dev:client\" \"npm:dev:admin\"",
    "build:client": "npm run build --prefix client",
    "build:admin": "npm run build --prefix admin",
    "build": "npm run build:client && npm run build:admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
